FROM alpine:latest

# Set working directory inside container
WORKDIR /mnt/host

# Create folder structure and file when container starts
CMD mkdir -p data/sub_data1 data/subdata_22 && touch instruction.txt